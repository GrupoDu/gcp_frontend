export type Register = {
  registers?: Register[];
  register_id: string;
  deadline: string;
  title: string;
  description: string;
  status: string;
  delivered_at: string;
  employee_uuid: string;
  product_uuid: string;
  deliver_observation: string;
};
